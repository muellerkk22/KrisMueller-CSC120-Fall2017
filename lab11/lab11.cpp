Select 3 numbers randomly 
    seed random
    The highest and lowest numbers are the ranges for the 3rd number
initiate countdown variable to ten 
    (used when random number not guessed)
tell user to select between the 2 numbers (highest random number and lowest random number) randomly generated
    read from stdin
if number is guessed correctly
    tell user
Otherwise, tell user whether random number is higher or lower than guess
    and subtract 1 from countdown variable if number not guessed

